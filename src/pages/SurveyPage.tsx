import SurveyCard from '@/components/survey/SurveyCard';

const SurveyPage = () => {
  return (
    <>
      <main className="bg-survey flex min-h-screen flex-col items-center justify-center">
        <SurveyCard />
      </main>
    </>
  );
};

export default SurveyPage;
